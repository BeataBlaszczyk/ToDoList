<%- include("header") -%>

<form id="formPassRecall" class="invisible" action="/recallPass" method="post">
  <div id="recallPassBg">
    <div id="x">
      <p onclick="closeWindow()">
        ✖️
        </p>
    </div>
  </div>
<div id="recallPass" class="">

  <input type="text" name="user" value="" placeholder="Put your username here">
  <button type="submit" name="button">

  REMAIND YOUR PASSWORD</button></div>
   </form>

<form action="/" method="post">


  <div class="loginForm">
    <img id="deer" src="/images/deer-back3.png" alt="">
    <p> Welcome on To Do List </p>

    <div class="logSign">

      <div class="login ">

          <h6 onclick="openWindow()" id="infoLog" value="<%=userErr%>"><%=dupa1%><br /><%=dupa2%> </h6>

        <input type="text" name="login" placeholder="Login" autocomplete="off" spellcheck="false">
        <input type="password" name="password" placeholder="Password" autocomplete="off">
        <button class="loginButton" type="submit" name="loginBut" value="l"><b></b>LOGIN</button>

      </div>

      <div class="signin login">
        <h6 id="infoLog" value="<%=userErr2%>"> <span><%=has%></span>
          <div id="passwordCorrect">
            <%=haslo1%><br /><%=haslo2%><br /><%=haslo3%> </div>
        </h6>

        <input id="loginS" type="text" name="loginS" placeholder="Login" autocomplete="off">
        <input type="password" name="passwordS" placeholder="Password" autocomplete="off">
        <input class="file2" type="file" id="file" name="avatar" onchange="PreviewImage()"  accept="image/*" />
        <label for="file"><img id="uploadPreview" name="no" style="width: 38px; height: 38px;" />
          <input type="txt" name="avatarPic" id="avatarPic" style="display: none" value=""></input>
          <p class="avatar">Choose your avatar </p>
        </label>

        <button class="loginButton" type="submit" name="loginBut" value="s"><b></b>SIGNIN</button>
      </div>
    </div>

  </div>

</form>

<script type="text/javascript">
  function openWindow(){

    document.getElementById("formPassRecall").classList.remove("invisible");

  }

  function closeWindow(){
    document.getElementById("formPassRecall").classList.add("invisible");

  }
  function PreviewImage() {

    var path = (window.URL || window.webkitURL).createObjectURL(event.target.files[0]);
    console.log('path', path);
    //avatarName = document.getElementById("loginS").value


      var file = document.getElementById("file").files[0];
      var reader = new FileReader();
      reader.onloadend = function() {
        console.log('Encoded Base 64 File String:', reader.result);
        var pathNew = reader.result;
        document.getElementById("avatarPic").setAttribute('value', pathNew);
      ;}

        reader.readAsDataURL(file);

  //  document.getElementById("file").setAttribute('src', path);


    var oFReader = new FileReader();

    oFReader.readAsDataURL(document.getElementById("file").files[0]);
    document.getElementById("uploadPreview").setAttribute("name", "chosen");
    oFReader.onload = function(oFREvent) {
      document.getElementById("uploadPreview").src = oFREvent.target.result;


    };
    var z = document.getElementById("uploadPreview").name;
    console.log("bbb" + z);
  };
</script>

<%- include("footer") -%>
